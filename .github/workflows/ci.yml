name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install
        run: pip install -e ".[dev,api]"
      - name: Lint
        run: ruff check src tests eval
      - name: Test
        run: pytest -q
      - name: Golden-case evaluation
        run: python -m eval.run_eval
